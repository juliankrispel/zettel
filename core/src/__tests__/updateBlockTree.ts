import { valueToTreeUpdates } from '../updateBlockTree'
import fromText from '../valueFromText'

describe('valueToTreeUpdates', () => {
  it('creates split block updates', () => {
    const value = fromText('hello][boing')
    const path = []
  })
})