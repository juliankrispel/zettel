import { BlockTree } from '../types'

export default function applyFragment(
  tree: BlockTree,
  path: number[],
  fragment: BlockTree
): BlockTree {
  fragment.blocks.forEach(block => {
  })
  return tree
}