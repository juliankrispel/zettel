import { ListState, BlockTree, Block } from './types'

export function updateBlockTree(flat: ListState, tree: BlockTree, start: number, end: number): BlockTree {
  // get minimum block range

  return tree
}